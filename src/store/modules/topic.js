import { TOPIC_LIST } from '../types'

const state = {
}

const getters = {
}

const actions = {
}

const mutations = {
    [TOPIC_LIST] (state, data) {
        console.log(data)
    }
}

export default {
    state,
    getters,
    actions,
    mutations
}
